[project]
name = "hello-agent"
version = "0.1.0"
description = "Hello-Agent: A Chapter-by-Chapter AI Exploration"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["core*", "examples*"]
